require 'test_helper'

class NavigationTest < ActionDispatch::IntegrationTest
  test "regist without email" do
    
  end

  test "create two account with same email, should fail" do
    assert true
  end

  test "create two account with same phone, should fail" do
    assert true
  end

end
