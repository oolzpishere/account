<p id="notice"><%= notice %></p>

<h1>User Views</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>
</table>
<%= current_user.phone if user_signed_in? %>
<br>

<%= link_to 'New User View', new_user_view_path %>
