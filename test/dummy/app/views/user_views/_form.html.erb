<%= form_with(model: user_view, local: true) do |form| %>
  <% if user_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_view.errors.count, "error") %> prohibited this user_view from being saved:</h2>

      <ul>
      <% user_view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
